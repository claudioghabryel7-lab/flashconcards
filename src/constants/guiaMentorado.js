/** Dias padrão de planejamento quando não há data da prova definida. */
export const DEFAULT_PLANNING_DAYS = 90

/** Hora (0–23) em America/Sao_Paulo para iniciar a geração diária. */
export const MENTORADO_DAILY_RELEASE_HOUR = 0
